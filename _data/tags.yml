# Add an entry, where the key represents a slug.
# Provide at least a `name` value and optionally `image`, `color` and `description`.
# Markdown is enabled for `description`, however don't use more than one paragraph (enforced by `>`)
#
# Example:
#
# ~~~
# mytag:
#   name: My Tag
#   description: >
#     *My Tag* is an example of how to define a custom tag.
# ~~~

polystack:
  name: Polystack
  image: /public/img/polystack.jpg
  color: '#949667'
  description: >
    Polystack is Chickadee Tech's exploration into UAV flight controller hardware.

adafruit:
  name: Adafruit
  image: /public/img/polystack.jpg
  color: '#949667'
  description: >
    Adafruit Industries is the main client of Chickadee Tech.
